% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[14pt,a4paper]{extarticle}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
% lmodern мешает выбору основного шрифта в XeLaTeX
% \usepackage{lmodern}
\ifPDFTeX\else
  % ГОСТ: Times New Roman (если установлен в системе); иначе Liberation (~Times)
  \IfFontExistsTF{Times New Roman}{%
    \setmainfont{Times New Roman}%
    \IfFontExistsTF{Arial}{\setsansfont{Arial}}{\setsansfont{Liberation Sans}}%
    \IfFontExistsTF{Courier New}{\setmonofont{Courier New}}{\setmonofont{Liberation Mono}}%
  }{%
    \setmainfont{Liberation Serif}%
    \setsansfont{Liberation Sans}%
    \setmonofont{Liberation Mono}%
  }%
  \setmathfont{TeX Gyre Termes Math}%
  \usepackage{polyglossia}%
  \setdefaultlanguage{russian}%
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
% ГОСТ 7.32-2017: абзацный отступ 1,25 см, без вертикальных отступов между абзацами (вместо parskip от pandoc)
\makeatletter
\@ifundefined{KOMAClassName}{%
  \setlength{\parindent}{12.5mm}%
  \setlength{\parskip}{0pt}%
}{%
  \KOMAoptions{parskip=false}%
  \setlength{\parindent}{12.5mm}}
\makeatother
\usepackage{indentfirst}
\usepackage{longtable,array}
\newcounter{none} % for unnumbered tables
\usepackage{multirow}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\usepackage{graphicx}
\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{% scales image to fit in text height/width
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi% select the smaller of both
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi%
}
% Set default figure placement to H (inline, no floating).
% Все рисунки и таблицы располагаются строго по месту в тексте.
\def\fps@figure{H}
\def\fps@table{H}
\makeatother
\usepackage{float}
\floatplacement{figure}{H}
\floatplacement{table}{H}
\ifLuaTeX
  \usepackage{luacolor}
  \usepackage[soul]{lua-ul}
\else
  \usepackage{soul}
\fi
% Переносы и «растяжка» строк (кириллица + длинные термины без дефисов)
\setlength{\emergencystretch}{5em plus 3em minus .5em}
\tolerance 2600
\pretolerance 500
\hfuzz 3pt
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{setspace}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

% Поля по ГОСТ 7.32-2017 (часто используется для ВКР/НИР; отдельной публичной методички матфака не найдено)
\usepackage[a4paper,left=30mm,right=15mm,top=20mm,bottom=20mm]{geometry}
\usepackage{pdflscape}

% Подписи таблиц/рисунков по ГОСТ: «Таблица N — Название» над таблицей справа,
% «Рисунок N — Название» под рисунком по центру.
\usepackage{caption}
\DeclareCaptionLabelSeparator{emdash}{~--- }
\captionsetup[figure]{name=Рисунок,labelsep=emdash,justification=centering,singlelinecheck=true}
\captionsetup[table]{name=Таблица,labelsep=emdash,justification=raggedright,singlelinecheck=false,position=above}

% Центрированные заголовки разделов/подразделов (по требованию: «по центру строк»).
% Используется в виде \crshead{ГЛАВА 3. ...} / \crssubhead{3.1. ...}.
% Дополнительно подавляет «висячие» заголовки в конце страницы (вместе с next-paragraph).
\usepackage{needspace}
\newcommand{\crshead}[1]{%
  \par\needspace{6\baselineskip}\vspace{1.2ex}%
  {\centering\bfseries #1\par}\vspace{0.6ex}\nopagebreak[4]}
\newcommand{\crssubhead}[1]{%
  \par\needspace{4\baselineskip}\vspace{0.8ex}%
  {\centering\bfseries #1\par}\vspace{0.4ex}\nopagebreak[4]}

\makeatletter
\newlength{\crs@tocnumw}
\setlength{\crs@tocnumw}{2.4em}
\newcommand{\crsTOCline}[3][0pt]{%
  \par\addvspace{0.32ex}%
  \noindent\hspace*{#1}%
  \parbox[t]{\dimexpr\linewidth-#1-\crs@tocnumw-0.5em}{\raggedright #2}%
  \hfill\parbox[t]{\crs@tocnumw}{\raggedleft #3}%
  \par}%
\newcommand{\crsTOCheading}{\par\vspace{1.2ex}\centering\bfseries\large}%
\makeatother

\author{}
\date{}

\begin{document}

\input{parts/01-title}
\input{parts/01a-abstract}
\input{parts/02-toc}
\input{parts/03-intro}
\input{parts/04-chapter1}
\input{parts/05-chapter2}
\input{parts/06-chapter3}
\input{parts/07-conclusion}
\input{parts/08-bibliography}
\input{parts/09-appendix1}
\input{parts/10-appendix2}

\end{document}
